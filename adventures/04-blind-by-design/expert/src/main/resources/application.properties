spring.application.name=demo

# OpenTelemetry SDK setup is provided by the OpenTelemetry Java Agent,
# which reads its configuration from otel.properties (next to pom.xml).
# Spring's Environment is not on the agent's lookup path, so do NOT put
# otel.* keys in this file — they will be silently ignored.
